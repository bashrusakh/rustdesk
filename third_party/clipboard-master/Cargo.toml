# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2018"
name = "clipboard-master"
version = "4.0.0-beta.6"
authors = ["Douman <douman@gmx.se>"]
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "Simple utility crate to monitor clipboard changes"
homepage = "https://github.com/DoumanAsh/clipboard-master"
readme = "README.md"
keywords = [
    "Windows",
    "winapi",
    "clipboard",
]
license = "MIT"
repository = "https://github.com/DoumanAsh/clipboard-master"

[lib]
name = "clipboard_master"
path = "src/lib.rs"

[[example]]
name = "listen"
path = "examples/listen.rs"

[[test]]
name = "shutdown"
path = "tests/shutdown.rs"

[target.'cfg(all(unix, not(any(target_os="macos", target_os="ios", target_os="android", target_os="emscripten"))))'.dependencies]
x11-clipboard = "0.9"

[target.'cfg(all(unix, not(any(target_os="macos", target_os="ios", target_os="android", target_os="emscripten"))))'.dependencies.wayland-client]
version = "0.31.4"

[target.'cfg(all(unix, not(any(target_os="macos", target_os="ios", target_os="android", target_os="emscripten"))))'.dependencies.wayland-protocols]
version = "0.32.6"
features = [
    "unstable",
    "staging",
    "client",
]
default-features = false

[target.'cfg(all(unix, not(any(target_os="macos", target_os="ios", target_os="android", target_os="emscripten"))))'.dependencies.wayland-protocols-wlr]
version = "0.3.2"
features = ["client"]
default-features = false

[target.'cfg(all(unix, not(any(target_os="macos", target_os="ios", target_os="android", target_os="emscripten"))))'.dependencies.wl-clipboard-rs]
version = "0.9"

[target.'cfg(all(unix, not(any(target_os="macos", target_os="ios", target_os="android", target_os="emscripten"))))'.dependencies.x11rb]
version = "0.13"
features = ["xfixes"]

[target.'cfg(target_os = "macos")'.dependencies]
objc = "0.2"
objc-foundation = "0.1"
objc_id = "0.1"

[target."cfg(windows)".dependencies]
windows-win = "3"
